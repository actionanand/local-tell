# LocalTell uses Android framework SQLite and telephony APIs directly.
# Keep BuildConfig so runtime data manifest configuration remains available.
-keep class com.actionanand.localtell.app.BuildConfig { *; }
